{
    "private": true,
    "name": "my-domain-package",
    "version": "0.1.0",
    "license": "MIT",
    "engines": {
        "node": ">=16"
    },
    "files": [
        "*"
    ],
    "sideEffects": false,
    "scripts": {
        "dev": "npm run storybook & npm run serve & npm run sandbox;",
        "storybook": "elsie storybook",
        "serve": "elsie serve & npm run sandbox",
        "sandbox": "http-server ./examples/html-host -c-1 -p 3000 -o",
        "lint": "elsie lint --max-warnings=0",
        "test": "elsie test",
        "build": "elsie build",
        "analyze": "ANALYZE=true elsie build"
    },
    "dependencies": {
        "@adobe/elsie": "latest",
        "@adobe/event-bus": "latest",
        "@adobe/fetch-graphql": "latest",
        "preact": "^10.3.2"
    },
    "devDependencies": {
        "dotenv": "^16.0.1",
        "http-server": "14.1.1"
    }
}