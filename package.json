{
    "private": true,
    "name": "my-domain-package",
    "version": "0.1.0",
    "license": "MIT",
    "engines": {
        "node": ">=16"
    },
    "files": [
        "*"
    ],
    "sideEffects": false,
    "scripts": {
        "dev": "npm run storybook & npm run serve",
        "storybook": "elsie storybook",
        "serve": "elsie serve & npm run sandbox",
        "lint": "elsie lint --max-warnings=0",
        "test": "elsie test",
        "build": "elsie build",
        "analyze": "ANALYZE=true elsie build",
        "sandbox": "http-server ./examples/html-host -c-1 -p 3000 -d -o"
    },
    "dependencies": {
        "@adobe/elsie": "alpha",
        "@adobe/event-bus": "alpha",
        "@adobe/fetch-graphql": "alpha",
        "@adobe/storefront-design": "alpha",
        "preact": "^10.3.2"
    },
    "devDependencies": {
        "http-server": "14.1.1",
        "jest": "^27.0.1"
    }
}